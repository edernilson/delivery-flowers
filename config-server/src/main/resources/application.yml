
server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/edernilson/microservice-config-repo.git
          search-paths: delivery-flowers/fornecedor